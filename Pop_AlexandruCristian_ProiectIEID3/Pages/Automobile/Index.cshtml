@page
@model Pop_AlexandruCristian_ProiectIEID3.Pages.Automobile.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Automobil[0].Nume)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Automobil[0].Firma)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Automobil[0].Model)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Automobil[0].Pret)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Automobil[0].DataAparitie)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Automobil[0].Vanzator.NumeVanzator)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Automobil) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nume)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Firma)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Model)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Pret)
            </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DataAparitie)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Vanzator.NumeVanzator)
                </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
